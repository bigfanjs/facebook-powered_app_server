{
  "properties": {
    "email": {
      "type": "string",
      "format": "email",
      "description": "email address"
    },
    "password": {
      "type": "string",
      "minLength": 6,
      "maxLength": 15,
      "description": "user password"
    }
  },
  "required": ["email", "password"]
}